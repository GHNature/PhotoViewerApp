# 1. 启用 AndroidX 基础支持
android.useAndroidX=true

# 2. 禁用非传递 R 类，解决 AGP 依赖解析冲突
android.nonTransitiveRClass=true

# 3. 关闭 Build Features 的默认混淆行为，避免提前解析依赖
android.defaults.buildfeatures.buildconfig=true

# 4. 增加 JVM 编译内存
org.gradle.jvmargs=-Xmx2048m
